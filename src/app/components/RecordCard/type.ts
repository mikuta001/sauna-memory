export type RecordCardProps = {
  title: string;
  rating: number;
  body: string;
  imageSrc?: string[];
  tagNames: string[];
};

export type RecordCardHeaderProps = {
  title: string;
  rating: number;
};
